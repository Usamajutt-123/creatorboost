import ContactClient from './ContactClient';

export const metadata = {
  title: 'Contact',
  description: 'Contact CreatorBoost support for account, campaign, and partnership help.',
};

export default function ContactPage() {
  return <ContactClient />;
}
